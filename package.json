{
  "name": "cc-chat-ui",
  "version": "26.9.13-alpha.1324",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:client": "vite --host 127.0.0.1",
    "dev:server": "node --watch server/index.mjs",
    "build": "tsc -b && vite build",
    "start": "node server/index.mjs",
    "typecheck": "tsc -b",
    "lint": "eslint src scripts server tests vite.config.ts",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && npm run lint && npm run build",
    "deploy": "npm run build && npx wrangler@4.130.0 deploy",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "npm run build && tauri build && node scripts/finalize-dmg.mjs",
    "tauri:check": "npm run build && npm run tauri:resources && cargo check --manifest-path src-tauri/Cargo.toml",
    "tauri:test": "npm run build && npm run tauri:resources && cargo test --manifest-path src-tauri/Cargo.toml",
    "tauri:resources": "node scripts/prepare-tauri-resources.mjs"
  },
  "dependencies": {
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "@tailwindcss/vite": "^4.3.3",
    "@tauri-apps/cli": "^2.11.4",
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^6.0.0",
    "eslint": "^9.39.0",
    "globals": "^16.5.0",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.0",
    "vite": "^8.3.0"
  }
}
